/**
 * A complete or partial mailing address.
 */
export type partial_shipping_address = {
    /**
     * Additional text about how to handle the shipment at this address.
     *
     */
    instructions?: string;
};
