/**
 * The possible shipment status values
 */
export declare enum shipment_status {
    PENDING = "pending",
    PROCESSING = "processing",
    LABEL_PURCHASED = "label_purchased",
    CANCELLED = "cancelled",
    AWAITING_PAYMENT = "awaiting_payment"
}
